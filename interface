# criando conexao
import psycopg2
conn = psycopg2.connect(host="localhost",
        database="Interface",
        user="postgres",
        password="/@JK@bb78")
print("Conexão com o Banco de Dados aberta com sucesso!")
cur=conn.cursor() 
cur.execute('''CREATE TABLE Produto2(CODIGO2 INT PRIMARY KEY NOT NULL,
            NOME2 TEXT NOT NULL,
            PRECO2 REAL NOT NULL);''') 

conn.commit() 
print("Tabela criada com sucesso!"); 
conn.close()